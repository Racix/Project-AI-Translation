apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.transcription.name }}
  labels:
    name: {{ .Values.transcription.name }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      name: {{ .Values.transcription.name }}
  template:
    metadata:
      labels:
        name: {{ .Values.transcription.name }}
    spec:
      containers:
        - name: {{ .Values.transcription.name }}
          image: "{{ .Values.transcription.repository }}:{{ .Values.transcription.tag | default .Chart.AppVersion }}"
          image: "{{ .Values.cuda.repository }}:{{ .Values.cuda.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.transcription.pullPolicy }}
          command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "{{ .Values.api.port_guest }}"]
          ports:
            - name: transcription
              containerPort: {{ .Values.transcription.port }}
              protocol: TCP
          envFrom:
          - configMapRef:
                name: env
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
