apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.frontend.name }}
  labels:
    name: {{ .Values.frontend.name}}
spec:
  type: LoadBalancer
  loadBalancerIP: 34.116.205.245
  ports:
    - port: {{ .Values.frontend.port }}
      targetPort: {{ .Values.frontend.port }}
      protocol: TCP
      name: http
  selector:
    name: {{ .Values.frontend.name}}


