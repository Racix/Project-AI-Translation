apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.translation.name }}
  labels:
    name: {{ .Values.translation.name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.api.port_guest}}
      targetPort: {{ .Values.api.port_guest }}
      name: translation
  selector:
    name: {{ .Values.translation.name }}