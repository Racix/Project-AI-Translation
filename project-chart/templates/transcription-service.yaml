apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.transcription.name }}
  labels:
    name: {{ .Values.transcription.name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.transcription.port }}
      targetPort: {{ .Values.api.port_guest }}
      name: transcription
  selector:
    name: {{ .Values.transcription.name }}