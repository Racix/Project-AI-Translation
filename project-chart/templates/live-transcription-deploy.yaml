apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.live-transcription.name }}
  labels:
    name: {{ .Values.live-transcription.name }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      name: {{ .Values.live-transcription.name }}
  template:
    metadata:
      labels:
        name: {{ .Values.live-transcription.name }}
    spec:
      containers:
        - name: {{ .Values.live-transcription.name }}
          image: "{{ .Values.live-transcription.repository }}:{{ .Values.live-transcription.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.live-transcription.pullPolicy }}
          command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "{{ .Values.api.port_guest }}"]
          ports:
            - name: transcription
              containerPort: {{ .Values.live-transcription.port }}
              protocol: TCP
          envFrom:
          - configMapRef:
                name: env
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
