apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.diarization.name }}
  labels:
    name: {{ .Values.diarization.name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.diarization.port }}
      targetPort: {{ .Values.api.port_guest }}
      name: diarization
  selector:
    name: {{ .Values.diarization.name }}